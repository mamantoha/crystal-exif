<!DOCTYPE html>
<html>

<body>
  <img src="data:<%= mime_type %>>;base64,<%= encoded %>" width="500">

  <table>
    <tbody>
      <% data.each do |key, value| %>
        <tr>
          <td><%= key %></td>
          <td><%= value %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</body>

</html>
